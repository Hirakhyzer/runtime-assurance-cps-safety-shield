# Reachability Shield

For each action candidate, the shield propagates an interval/box enclosure through the affine model. Multi-step prediction uses a hold-action assumption. Box arithmetic is intentionally conservative and may cause wrapping/over-approximation. Increasing the horizon can therefore improve anticipation but also increase unnecessary intervention.
