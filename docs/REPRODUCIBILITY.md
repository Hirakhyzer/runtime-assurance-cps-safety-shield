# Reproducibility

Use Python 3.10+ and install `.[dev]`. Tests and simulations use explicit random seeds. Generated benchmark outputs belong in `results/` and are excluded from version control by default. Record package version, seed list, horizon, action-grid density, uncertainty radii, and domain parameters with every reported experiment.
