# Cyber and Fault Model

Cyber/fault effects are represented only as bounded mathematical uncertainty added to synthetic state or action channels. Examples include bounded sensor offset, command perturbation, and stale-estimate inflation. No real protocol, vendor device, credential, exploit payload, or live target is modeled.
